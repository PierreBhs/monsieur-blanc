import { describe, expect, it } from "vitest";

// TODO: Add real UI tests for the App setup flow (add/remove players, role
// counters, starting a round). jsdom + @testing-library are configured, so a
// future test can render(<App />) and drive it via user-event.
describe("App", () => {
  it.todo("walks through the game setup and starts a round");

  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
